{
    "version": "0.2.0",
    "configurations": [
            {
            "type": "node-terminal",
            "request": "launch",
            "name": "Debug Nest",
            "command": "yarn run start:debug",  // or "yarn start:debug"
            "cwd": "${workspaceFolder}"
        }
    ]
}
